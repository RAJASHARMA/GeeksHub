<header>

	<nav class="navbar navbar-expand-lg navbar-light bg-light">
		<div class="col-md-4">
			<a class="navbar" href="<%= root_path %>"><span class="geeks-hub-font">GeeksHub</span></a>
		</div>
	  	

	  	<div class="collapse navbar-collapse container-fluid col-md-8" id="navbarMenu">
		  <ul class="navbar-nav ml-auto">
		    
		    <li class="nav-item">
		      <%= link_to 'Create Article', new_article_path, class: 'nav-link' %>
		    </li>
		    <ul class="navbar-nav ml-auto">
		      <% if user_signed_in? %>
		      	<% _user_name = current_user.name ?  current_user.name : current_user.email.split('@')[0]  %>
		        <li class="nav-item dropdown float-right">
		          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
		            <%= _user_name + '(' +current_user.roles.pluck(:name).join(",") +')' %>
		          </a>
		          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
		            <%= link_to 'Edit Profile', edit_user_registration_path, class: 'dropdown-item' %>
		            <div class="dropdown-divider"></div>
		            <%= link_to 'My Articles', articles_user_articles_path, class: 'dropdown-item' %>
		            <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
		          </div>
		        </li>
		      <% else %>
		        <li class="nav-item">
		          <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
		        </li>
		      <% end %>
		    </ul>
		  </ul>
		</div>
	</nav>
	
</header>