<div class="">
    <h2>Log in</h2><br>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
          <div class="form-group row">
              <div class="col-md-2"><%= f.label :email, class: 'label' %></div>
              <div class="col-md-8"><%= f.email_field :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email" }, class: 'form-control' %></div>
          </div>
          
          <div class="form-group row">
              <div class="col-md-2"><%= f.label :password, class: 'label' %></div>
              <div class="col-md-8"><%= f.password_field :password,
                    required: false,
                    input_html: { autocomplete: "current-password" }, class: 'form-control' %></div>
          </div>
          
          <div class="form-group">
              <%= f.check_box :remember_me, as: :boolean if devise_mapping.rememberable? %> Remember me
          </div>
              
      </div>

      <div class="form-actions">
        <%= f.submit "Log in", class: 'btn btn-success' %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
</div>